# Release Process

Release can be triggered manually through GitHub Actions or by pushing a semantic version tag. Artifacts include bootloader, partition table, application binary, map/ELF when available, manifest, and release notes.
