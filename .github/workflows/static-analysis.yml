name: Static Analysis
on:
  pull_request:
  workflow_dispatch:
jobs:
  formatting:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: clang-format check
        run: |
          python3 tools/ci/check_clang_format.py
