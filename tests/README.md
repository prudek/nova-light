# Tests

- `unit/`: host or ESP-IDF component unit tests.
- `integration/`: integration tests with built firmware or simulated services.
- `hil/`: human-gated hardware-in-the-loop tests.
- `fixtures/`: logs, sample manifests, and reproducible data.
